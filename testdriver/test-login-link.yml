version: 4.2.24
session: 67e3f6ed3758c7759787bcfc
steps:
  - prompt: Click the "LOGIN" link in the header.
    commands:
      - command: hover-text
        text: LOGIN
        description: login link in the header
        action: click
  - prompt: Assert that the login page loads.
    commands:
      - command: assert
        expect: the text "Log in" is visible on the page
  - prompt: >-
      Check for the presence of username and password fields.  # Use the content
      of the file from the environment
    commands:
      - command: assert
        expect: the text "Email address" is visible on the page
      - command: assert
        expect: the text "Password" is visible on the page
