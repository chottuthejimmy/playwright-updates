version: 4.2.20
session: 67dc19bb559da55d39a3216d
steps:
  - prompt: Assert that the cookie consent banner is displayed.
    commands:
      - command: assert
        expect: the "Accept Cookies" button is visible
  - prompt: Click "Accept" on the cookie consent banner.
    commands:
      - command: hover-text
        text: Accept Cookies
        description: button on the cookie consent banner
        action: click
  - prompt: >-
      Assert that the banner disappears after acceptance.  # Use the content of
      the file from the environment
    commands:
      - command: assert
        expect: the "Accept Cookies" button is not visible
