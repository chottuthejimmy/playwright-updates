version: 4.2.24
session: 67e3d5a7e166e87893ec2130
steps:
  - prompt: Click on "Solutions" in the navigation bar.
    commands:
      - command: hover-text
        text: Solutions
        description: solutions in the navigation bar
        action: click
  - prompt: Assert that the Solutions page is displayed correctly.
    commands:
      - command: assert
        expect: the Solutions dropdown menu is visible
  - prompt: >-
      Verify that no errors are present on the page.  # Use the content of the
      file from the environment
    commands:
      - command: assert
        expect: >-
          no error messages like "404", "Error", or "Not Found" are visible on
          the page
