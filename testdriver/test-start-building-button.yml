version: 4.2.24
session: 67e3ca433758c7759787b9dc
steps:
  - prompt: Click on the "Start Building" button.
    commands:
      - command: hover-text
        text: Start Building
        description: button to initiate building process on the webpage
        action: click
  - prompt: Assert that the navigation to the start building page is successful.
    commands:
      - command: assert
        expect: the text "Register for free" is visible
  - prompt: >-
      Assert there are no errors on the page.  # Use the content of the file
      from the environment
    commands:
      - command: assert
        expect: the text "Error" is not visible
  - prompt: >-
      Assert there are no errors on the page.  # Use the content of the file
      from the environment
    commands:
      - command: assert
        expect: the text "error" is not visible
  - prompt: >-
      Assert there are no errors on the page.  # Use the content of the file
      from the environment
    commands:
      - command: assert
        expect: the text "error" is not visible
  - prompt: >-
      Assert there are no errors on the page.  # Use the content of the file
      from the environment
    commands:
      - command: assert
        expect: the text "error" is not visible
