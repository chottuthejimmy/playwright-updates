version: 4.2.20
session: 67dc0027559da55d39a31f22
steps:
  - prompt: Focus the browser window.
    commands:
      - command: focus-application
        name: Google Chrome
  - prompt: Focus the browser window.
    commands:
      - command: focus-application
        name: Google Chrome
  - prompt: Focus the browser window.
    commands:
      - command: focus-application
        name: Google Chrome
  - prompt: Click on "Accept Cookies" in the cookie consent banner.
    commands:
      - command: focus-application
        name: Google Chrome
      - command: hover-text
        text: Accept Cookies
        description: button in the cookie consent banner
        action: click
  - prompt: Assert that the banner disappears.
    commands:
      - command: assert
        expect: the cookie consent banner is not visible
  - prompt: >-
      Confirm no errors are shown after acceptance.  # Use the content of the
      file from the environment
    commands:
      - command: assert
        expect: no error messages are visible on the webpage
