version: 4.2.24
session: 67e3ca4de166e87893ec2028
steps:
  - prompt: Hover over the "Products" link.
    commands:
      - command: hover-text
        text: Products
        description: link in the navigation bar
        action: hover
  - prompt: Assert that a dropdown menu appears.
    commands:
      - command: assert
        expect: the dropdown menu under "Products" is visible
  - prompt: >-
      Click on an item in the dropdown and assert the navigation is correct.  #
      Use the content of the file from the environment
    commands:
      - command: hover-text
        text: Overview
        description: item in the dropdown under PUBNUB PLATFORM
        action: click
      - command: assert
        expect: the page navigates to the Overview section
