version: 4.2.20
session: 67db038a1b8e3a3c383bcc23
steps:
  - prompt: Click on the "Accept Cookies" button in the cookie consent banner.
    commands:
      - command: hover-text
        text: Accept Cookies
        description: button in the cookie consent banner at the bottom of the page
        action: click
  - prompt: >-
      Assert that the banner disappears and cookies are accepted.  # Use the
      content of the file from the environment
    commands:
      - command: assert
        expect: the cookie consent banner is not visible
