version: 4.2.24
session: 67e4022fe166e87893ec244f
steps:
  - prompt: Click on "Products" in the navigation menu.
    commands:
      - command: hover-text
        text: Products
        description: products link in the navigation menu
        action: click
  - prompt: Assert that the Products page loads successfully.
    commands:
      - command: assert
        expect: PUBNUB PLATFORM is visible on the Products page
  - prompt: >-
      Verify there are no visible errors.  # Use the content of the file from
      the environment
    commands:
      - command: assert
        expect: no visible errors like "404", "Error", or "Not Found" on the page
  - prompt: >-
      Verify there are no visible errors.  # Use the content of the file from
      the environment
    commands:
      - command: assert
        expect: no visible error messages on the Products page
  - prompt: >-
      Verify there are no visible errors.  # Use the content of the file from
      the environment
    commands:
      - command: assert
        expect: no visible error messages on the Products page
  - prompt: >-
      Verify there are no visible errors.  # Use the content of the file from
      the environment
    commands:
      - command: assert
        expect: no visible error messages on the Products page
